# TODO

- [x] Add `audioUrl` field to `Nasyid` type (types/index.ts)
- [x] Add MP3 URL for nasyid id `1` in `lib/data.ts`
- [x] Update `NasyidSection.tsx` so Play button actually plays `track.audioUrl` using an `<audio>` element


- [x] Verify `npm run dev` and test that “Selawat Nabi” can be played


